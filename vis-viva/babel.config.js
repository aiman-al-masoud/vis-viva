module.exports = {
    //prevent babel from changing node import syntax to ESM syntax and causing a syntax mismatch with static imported resources using node syntax.
    "sourceType": "unambiguous"
}
